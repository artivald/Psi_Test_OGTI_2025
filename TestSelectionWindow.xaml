<Window x:Class="PsychologicalTestsApp.TestSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Тесты" Height="600" Width="500"
        Background="#F0F0F0"
        Icon="images/3.png">
    <Window.Resources>
        <Style TargetType="Button" x:Key="ActionButton">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Width" Value="200"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="20">
        <StackPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <Image Source="Images/2.png" Stretch="UniformToFill" Height="188" Margin="0,0,0,10"/>
            <TextBlock Text="Выберите тест:" Margin="0,10,0,10" FontSize="16" FontWeight="Bold"/>
            <ListBox x:Name="TestsListBox" Margin="0,0,0,20" Height="218" SelectionMode="Single" />
            <Button Content="Пройти тест" Style="{StaticResource ActionButton}" Click="TakeTestButton_Click" 
                    HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>